{
  "name": "saves-api",
  "version": "1.0.0",
  "description": "Saves App API - Personal Finance Tracker",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create saves-db",
    "db:migrate": "wrangler d1 execute saves-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute saves-db --remote --file=./schema.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.11.3",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0"
  }
}
